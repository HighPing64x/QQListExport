<Window x:Class="QQListExport.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QQListExport"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="QQ群管理助手 - 制作者: highping QQ:3948185609" Height="700" Width="1100">
    <Window.Resources>
        <local:HtmlDecodeConverter x:Key="HtmlDecode" />
    </Window.Resources>
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 顶栏 -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,6">
            <Button x:Name="BtnOpenLogin" Content="打开登录页" Width="100" Margin="0,0,6,0" Click="BtnOpenLogin_Click" />
            <Button x:Name="BtnCookieLogin" Content="Cookie登录" Width="100" Margin="0,0,6,0" Click="BtnCookieLogin_Click" />
            <Button x:Name="BtnGetGroups" Content="获取群列表" Width="100" Margin="0,0,6,0" Click="BtnGetGroups_Click" IsEnabled="False" />
            <Button x:Name="BtnFetchSelectedMembers" Content="获取群内成员列表" Width="130" Margin="0,0,6,0" Click="BtnFetchSelectedMembers_Click" IsEnabled="False" />
            <Button x:Name="BtnGetFriends" Content="获取好友列表" Width="100" Margin="0,0,6,0" Click="BtnGetFriends_Click" IsEnabled="False" />
            <Separator Width="12" />
            <Button x:Name="BtnExportJson" Content="导出JSON" Width="90" Margin="6,0,6,0" Click="BtnExportJson_Click" IsEnabled="False" />
            <Button x:Name="BtnExportXml" Content="导出XML" Width="90" Margin="6,0,6,0" Click="BtnExportXml_Click" IsEnabled="False" />
            <Button x:Name="BtnExportCsv" Content="导出CSV" Width="90" Margin="6,0,6,0" Click="BtnExportCsv_Click" IsEnabled="False" />
            <Button x:Name="BtnExportExcel" Content="导出Excel" Width="90" Margin="6,0,6,0" Click="BtnExportExcel_Click" IsEnabled="False" />
            <Button x:Name="BtnExportPng" Content="导出图片" Width="90" Margin="6,0,6,0" Click="BtnExportPng_Click" IsEnabled="False" />

            <Separator Width="12" />
            <TextBlock VerticalAlignment="Center" Text="显示：" Margin="6,0,6,0" />
            <ComboBox x:Name="ViewSelector" Width="160" SelectionChanged="ViewSelector_SelectionChanged">
                <ComboBoxItem Content="群汇总" IsSelected="True" />
                <ComboBoxItem Content="好友列表" />
            </ComboBox>
        </StackPanel>

        <!-- 主要内容 -->
        <Grid Grid.Row="1">
            <wv2:WebView2 x:Name="WebView" Visibility="Collapsed" NavigationCompleted="WebView_NavigationCompleted" />
            <DataGrid x:Name="DataGridMain" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Extended" SelectionUnit="FullRow">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="复制选中行" Click="MenuItemCopySelected_Click" />
                        <MenuItem Header="复制QQ号" Click="MenuItemCopyUin_Click" />
                    </ContextMenu>
                </DataGrid.ContextMenu>
            </DataGrid>
        </Grid>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="2" Margin="0,6,0,0">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="就绪" />
            </StatusBarItem>
            <Separator />
            <StatusBarItem HorizontalAlignment="Right">
                <!-- 删的人死妈了 -->
                <TextBlock Text="制作者: highping QQ:3948185609" Foreground="Gray" FontStyle="Italic" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>